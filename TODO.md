# TODO — FitWeaver

## ✅ FIXED 2026-09-06 — финальные exe

- ✅ FIXED: пересобраны dist/FitWeaver.exe и dist/garmin-fit-cli.exe с текущими исправлениями.
- ✅ FIXED: GUI жив 8 секунд, CLI --help успешен; 364 теста passed в build-окружении.
- ✅ FIXED: сохранённый профиль dist использует localhost и qwen3.8-27b@iq3_xxs.
- Следующая сессия: пользовательская проверка полного плана через новый exe.

## ✅ FIXED 2026-09-06 — localhost LM Studio

- ✅ FIXED: defaults и GUI-профиль используют http://127.0.0.1:1234 и загруженную qwen3.8-27b@iq3_xxs.
- ✅ FIXED: loopback-запрос SDK обходит Windows proxy; remote proxy остаётся включённым.
- ✅ FIXED: auto использует обнаруженный native LM Studio API с reasoning=off/store=false.
- ✅ FIXED: бюджет native-ответа учитывает загруженный контекст; ошибка переполнения видна в результате.
- ✅ FIXED: CLI не превращает regex-count в обязательное требование к свободному тексту.
- ✅ FIXED: distance_km пересчитывается из полностью дистанционных шагов и repeat.
- ✅ FIXED: live свободный план на 3 тренировки, интервалы и ссылка между днями — первая попытка, 21.89 с, без reasoning.
- ✅ FIXED: 364 теста passed, Ruff/compileall чистые.

Актуальные следующие задачи: полный исходный план пользователя, пересборка обоих
exe, GUI smoke. LAN-сервер больше не используется; восстановление LAN не требуется.

## Исправления аудита 2026-09-06

- ✅ FIXED: удалено угадывание repeat и неверного back_to_offset.
- ✅ FIXED: единый путь генерации полного текста вместо автоматической сегментации.
- ✅ FIXED: свободный человеческий ввод не ограничен шаблоном; regex-count не блокирует результат.
- ✅ FIXED: общие инструкции и ссылки между днями сохраняются в запросе целиком.
- ✅ FIXED: retry = 2 полных попытки; исправляющий запрос содержит предыдущий ответ.
- ✅ FIXED: think-prefix, fallback при неполном chat, truncation, лимит вывода 16384.
- ✅ FIXED: корректный YAML с произвольным порядком ключей больше не портится очисткой.
- ✅ FIXED: единый auto для GUI/doctor/benchmark; openai/pydantic объявлены в зависимостях.
- ✅ FIXED: ISO-недели, intensity, темп восстановления и суммы дистанций примеров промпта.
- ✅ FIXED: Python 3.12.10 установлен, .venv создан; 354 теста passed, Ruff/compileall чистые.
- ✅ FIXED: тест Telegram-ветки без подключения изолирован от наличия необязательного garmin-auth.

Решение пользователя: вход — произвольный текст человека. Проверки regex-фактов
оставлены диагностическими предупреждениями, а не обязательным входным контрактом.
Это заменяет предложение аудита ужесточить все такие проверки. Структура выходного
YAML и repeat проверяются строго; понимание смысла остаётся задачей модели.

### Next tasks

- ✅ FIXED: вместо недоступного LAN используется localhost по указанию пользователя.
- [ ] Проверить live Qwen на полном свободном плане пользователя и ссылках между днями.
- [ ] Пересобрать dist/FitWeaver.exe и dist/garmin-fit-cli.exe после live-проверки.
- [ ] Рассмотреть structured output сервера, не вводя шаблон для человеческого текста.
- [ ] Улучшать диагностическую проверку семантики на разнообразном корпусе свободных планов.


_Обновлено: 2026-09-04 (LAN Qwen IQ4_XS, интервалы, Markdown-план, пересборка exe, Ctrl+V)_

## ✅ Закрыто 2026-09-03 — сборка exe и локальная Qwen

- ✅ Обнаружен и проверен LAN OpenAI-compatible сервер `192.168.1.107:8080/v1` с моделью
  `Qwen3.8-27B-UD-IQ3_XXS.gguf`; Ollama не используется.
- ✅ Defaults GUI/CLI/doctor/API/benchmark и сохранённый GUI-профиль переключены на LAN Qwen.
- ✅ GUI использует `openai_mode=completions` для этого сервера; короткий completions smoke
  успешен, chat endpoint возвращает пустой ответ.
- ✅ Добавлено отсечение reasoning-suffix Qwen и regression-тест.
- ✅ Собраны `dist/FitWeaver.exe` и `dist/garmin-fit-cli.exe`; GUI жив после 8 секунд.
- ✅ Полный regression suite: 323 passed; compileall и Ruff чистые.
- ⚠️ Полный план дошёл до Qwen, но IQ3_XXS вернула повреждённый YAML (`hrдo`); встроенная
  валидация корректно отклонила результат.
- ⚠️ Визуальный Computer Use smoke невозможен из-за отсутствующего native pipe helper.
- ✅ Исправлена вставка Ctrl+V в поле «Текст плана тренировок»: используется штатный `<<Paste>>`
  Tk с Windows KeyPress-биндингами; пересобраны `FitWeaver.exe` и `garmin-fit-cli.exe`.

## Next tasks после сборки

- ✅ Defaults переключены на загруженную модель `Qwen3.8-27B-UD-IQ4_XS.gguf`.
- ⚠️ Повторить интервал-тест после загрузки BF16: IQ4_XS всё ещё иногда теряет `repeat`.
- ✅ Добавить адресный retry сегмента при пропущенных шагах/фактах.
- ✅ Показывать ошибки LLM в GUI вместо ложного «готово — 0 тренировок».
- ✅ Восстанавливать очевидный `type` шага по его полям перед валидацией.
- ✅ Исправить разбор Markdown-заголовков `### дата, день — название`, включая отступы и запятую.
- ✅ Не считать дни только с силовой/сауной/ОФП отдельными беговыми тренировками.
- ✅ Усилить промпт: точное количество блоков, сохранение фактов, интервалы и повторения.
- ✅ Синхронизировать `easy_drills`, прогнать 329 тестов и пересобрать оба exe.
- ✅ Исправить распознавание заголовков `дата + день недели + название` и включить
  сегментированную генерацию для таких планов; добавлены тесты, exe пересобраны.
- ✅ Отсекать `<think>` из completion-ответа до YAML-разбора; не принимать ответы без `steps`.
- Повторить генерацию после выбора ещё более качественной модели или настройки LM Studio.
- Повторить визуальный GUI/exe smoke при доступном Computer Use helper.

## ✅ Закрыто 2026-09-02 — проверка консистентности проекта

- ✅ CLAUDE.md больше не ссылается на несуществующий в нём «Журнал сессий» — журнал
  живёт в `AGENTS.md`, версии в `version.txt`.
- ✅ `Next tasks` в CLAUDE.md и AGENTS.md синхронизированы с TODO.md (пункты про статус
  валидации YAML и редактирование закоммиченных тренировок уже закрыты).
- ✅ Счётчики тестов в доках приведены к факту: 313 passed, 9 skipped, 322 collected.
- ✅ `docs/YAML_GUIDE.md`: пути `Scripts/llm/*.yaml` → `src/garmin_fit/llm/*.yaml`.
- ✅ Снят BOM с `docs/CHANGELOG.md`.
- ✅ `ruff check` чист на `src/`, `fitweaver_gui.py`, `Scripts/`, `garmin_fit/`, `tests/`:
  убран неиспользуемый `tempfile`, отсортированы импорты, удалена мёртвая переменная в
  `test_plan_store.py`; `E701` внесён в ignore как осознанный табличный стиль GUI.
- ✅ `ruff target-version` = `py310`, совпадает с `requires-python`.
- ✅ Legacy-шимы `Scripts/` и `Scripts/llm/` импортируются после удаления дублей данных
  (пункт из «Next tasks после аудита» закрыт).

## Next tasks после проверки консистентности

- Headless/manual smoke-тест режима Предпросмотр/Применить и автовалидации YAML.
- End-to-end Calendar dry-run/upload payload tests.
- Remote-hosting smoke-тест Plan API (API и GUI/бот на разных машинах).
- PyInstaller-сборка после очистки `Scripts/llm/` data (шимы проверены, exe — нет).

## ✅ Закрыто 2026-09-02 — фикс test-сьюита (Python 3.10) + синхронизация доков

- ✅ Полный `pytest tests/` завершается на Python 3.10 без `--ignore`: 313 passed,
  9 skipped, ~1.5s. Корень зависания — stdlib `asyncio.to_thread` + teardown
  per-test loop; в `test_telegram_bot_cancel.py` он нейтрализован в `setUp`/`tearDown`.
- ✅ docstring `check_fit.py`: «Timestamp is reasonable» → «Has a time_created field»
  (проверка разумности даты отсутствует, оставлен только presence-check).
- ✅ `version.txt` и `docs/CHANGELOG.md` синхронизированы с GUI-фазой v10.4.1
  (2026-07-11) и post-audit фиксами.

## ✅ Закрыто 2026-09-02 — первый UX-этап GUI

- ✅ Скрытый `Dry-run` заменён на явный режим `Предпросмотр / Применить` рядом с
  Garmin-действиями; кнопки и бейдж показывают текущий режим.
- ✅ Для боевой загрузки и удаления добавлены подтверждения со сводкой контекста.
- ✅ После загрузки YAML автоматически запускается валидация; успешная проверка
  отдельно разрешает сборку FIT и загрузку в Garmin.
- ✅ Добавлен степпер процесса `Профиль → План → Проверен → FIT → Garmin` и
  состояния для реально собранного FIT и применённого Garmin-действия.
- ✅ Убраны дублирующие действия из вкладки LLM; оставлено сохранение YAML как
  текущего плана. Символ `↺` заменён на понятную кнопку `Обновить`.
- ✅ `python3 -m py_compile` и полный набор из 322 тестов пройдены, 9 skip.
- ✅ Верхний контекст дополнен текущим профилем и причиной следующего действия.
- ✅ Над подробным логом добавлен компактный блок результата операции.
- ✅ В результат добавлены действия «Открыть папку FIT», «Перейти к Garmin» и
  «Перейти к применению» после предпросмотра.
- ✅ В Simple mode подробный лог свёрнут по умолчанию; в Expert mode он раскрыт.
- ✅ Названия режимов LLM уточнены для обычного пользователя.
- ✅ В календарь, поля периода и Конструктор добавлены контекстные подсказки.

## Next tasks после первого UX-этапа

- Провести headless/manual smoke-тест нового режима и автоматической валидации YAML.
- Зафиксировать единственное каноническое место Garmin-действий без дублирования.
- Провести GUI/exe smoke-тест после проверки всех состояний Simple/Expert mode.
- Доработать точные предусловия кнопок, действия после успеха и улучшения вкладок.

## ✅ Закрыто 2026-09-02 — технический аудит и точечные исправления

- ✅ Calendar workflow теперь выполняет repair + validation до построения payload.
- ✅ Pace-константы имеют единое разрешение в validator/REST mapper.
- ✅ Исправлено относительное вычисление nested repeat и добавлен regression-тест.
- ✅ Standalone `save_workout()` получает collision-safe serial/timestamp из state manager.
- ✅ Исправлены benchmark paths, package version, reset-state locking и GUI callback.
- ✅ Удалены подтверждённые dead functions, дубли данных `Scripts/llm/` и generated caches.

## Next tasks после аудита

- Проверить async-тесты на Python 3.11+ и формально определить minimum supported Python.
- Добавить end-to-end Calendar dry-run/upload payload tests.
- Отдельно проверить legacy shims и PyInstaller после очистки `Scripts/llm/` data.

---

## ✅ Закрыто 2026-07-07 (продолжение) — Фаза F: широкая лог-панель + понятные подсказки

- ~~Лог вывода команд читаем и виден на любой вкладке~~ ✅ — `body` теперь
  `ttk.PanedWindow` с тремя перетаскиваемыми панелями (сайдбар/notebook/лог), лог-панель
  растягивается на всю высоту (`fill="both", expand=True"`), не спрятана внутри вкладки
  Календарь.
- ~~Понятные ошибки с подсказками для обычного пользователя~~ ✅ — `_ERROR_HINTS` +
  `_maybe_hint()`: при совпадении строки лога с известной ошибкой (YAML не найден, обрыв
  LLM, таймаут, ошибка Garmin-логина и т.д.) добавляется подсказка на понятном языке +
  toast на 6с. Не трогает текст самих CLI-ошибок — чисто презентационный слой в GUI.
- Модальное окно для полного лога — сознательно не делали (пользователь выбрал приоритет
  между тремя вариантами).

Автотестов на GUI нет (см. Фазу C), ручной smoke-тест пройден. 309 тестов backend не
тронуты.

## ✅ Закрыто 2026-07-07 (продолжение) — Фаза E: упаковка GUI в standalone .exe

- ~~Упаковать GUI в .exe для пользователей без Python~~ ✅ — `packaging/build.ps1` →
  `dist/FitWeaver.exe` + `dist/garmin-fit-cli.exe` (обязательно вместе, в одной папке).
- Два фризед-безопасных фикса пути (`config.py`, `fitweaver_gui.py`): состояние
  (`Plan/`, `profiles/`, `.gui_session.json`) резолвится от папки exe, не от временной
  распаковки — переживает перезапуск.
- `_cli_command()`: сайдбар-кнопки (сборка FIT, Garmin upload/delete, ПРОДВИНУТЫЕ) во
  frozen-режиме зовут `garmin-fit-cli.exe`-соседа вместо `python -m garmin_fit.cli`
  (во фризед exe `sys.executable` — сам GUI, не интерпретатор).
- **Найден и исправлен реальный баг** (не вручную — через smoke-тест собранного exe):
  легаси compatibility-bridge пакет `garmin_fit/` в корне репо (почти пустой, только
  `__init__.py` + пустая `llm/`) сбивал PyInstaller при разрешении лениво импортируемых
  модулей (`plan_store`, `workout_builder` и т.д.) — тихо выпадали из сборки без единой
  ошибки. Фикс: `build.ps1` собирает из изолированной копии-стейджинга без корневого
  `garmin_fit/`, а не из живого дерева исходников. Подробности в CLAUDE.md → "Desktop
  packaging (.exe)".
- Только GUI — бот и Plan API остаются Python-сервисами (осознанно, подтверждено вопросом).

Автотестов на упаковку нет (ручной build+smoke-тест реального .exe — запуск, Конструктор,
`garmin-fit-cli.exe validate-yaml` на тестовом YAML). 307 тестов backend не тронуты.

## ✅ Закрыто 2026-07-07 — Фаза D: простой/экспертный режим интерфейса

- ~~Простой/экспертный режим интерфейса~~ ✅ — чекбокс "Экспертный режим" в сайдбаре,
  `self.ui_mode` (`"simple"`/`"expert"`), персистится в `.gui_session.json`.
- Простой режим (по умолчанию) скрывает: секцию "ПРОДВИНУТЫЕ" в сайдбаре, панели
  подключения LLM на вкладке LLM (за кнопкой "Настроить подключение"), поле "Лимит" на
  вкладке Garmin Connect.
- Область сознательно ограничена лёгким вариантом — Конструктор и Календарь не тронуты
  (упрощение палитры блоков/повторов лишило бы простой режим интервальных тренировок).
- Попутно исправлен потенциальный баг с порядком `pack()`/`pack_forget()` (виджет уезжал бы
  в конец списка потомков при повторном показе) — все повторные packs используют `before=`.

Автотестов на GUI нет (нет тестовой инфраструктуры для Tkinter — то же решение, что в Фазе C),
ручной smoke-тест через headless `App()` пройден. 307 тестов (backend не тронут) по-прежнему проходят.

## ✅ Закрыто 2026-07-07 — Фаза C: единый Plan API + бот-клиент

- ~~Единый API над `plan_service.py`~~ ✅ — `src/garmin_fit/api/` (FastAPI), 3 эндпоинта
  (`/v1/generate-draft`, `/v1/apply-sbu-choice`, `/v1/health`), auth через `X-Api-Token`,
  rate limit (token bucket per-токен). `pip install -e ".[api]"`, `garmin-fit-api`.
- Бот теперь тонкий клиент API (`api_client.PlanApiClient`), не строит `UnifiedLLMClient`
  сам — конечный автомат и вся Telegram-специфика не тронуты.
- GUI: новый режим "LLM автора" в LLM-вкладке — подключение к чужому Plan API вместо
  обязательной настройки своей LLM.
- Решение: бот и API всегда общаются по HTTP, даже на одной машине — без флага для
  in-process обхода (см. CLAUDE.md → Journal → 2026-07-06/07 за обоснование).
- Ручной smoke-тест реального сервера через curl — auth и health подтверждены end-to-end.

20 новых тестов, итого 307 проходят.

## 🟢 Новые задачи (после Фазы E)

### ✅ GUI/UX: ручная smoke-проверка exe — 2026-07-11
Пользователь самостоятельно проверил запуск и базовый сценарий профиля, YAML и редактирования
тренировки в собранном exe. Следующий ручной фокус — перемещение шагов вокруг repeat.

### ✅ GUI/UX: безопасное удаление шагов вокруг repeat — 2026-07-11
В режиме редактирования можно удалять не-якорные шаги с автоматическим пересчётом repeat-offset.
Удаление якоря запрещается, чтобы не повредить структуру повторов.

### ✅ GUI/UX: редактирование существующих тренировок — базовый режим — 2026-07-11
Из календаря тренировку можно открыть в Конструкторе, изменить шаги и сохранить обратно в YAML.
Изменения проходят через транзакционный `PlanStore`; редактирование структуры repeat будет
расширяться отдельными безопасными сценариями.

### ✅ GUI/UX: onboarding-подсказка «ДАЛЬШЕ» — 2026-07-11
Верхняя карточка теперь подсказывает следующий шаг и ведёт к нему одной кнопкой.

### ✅ GUI/UX: версия и безопасная проверка обновлений — 2026-07-11
Версия приложения показана в шапке. GUI проверяет GitHub releases в фоне и по ручной кнопке,
но не скачивает и не устанавливает обновления автоматически.

### ✅ GUI/UX: экспорт диагностики Garmin — 2026-07-10
Историю операций можно сохранить в JSON для поддержки. Экспорт не содержит пароль или token-cache.

### ✅ GUI/UX: история операций Garmin — 2026-07-10
На вкладке Garmin Connect отображаются последние 8 проверок, загрузок и удалений с результатом.
История разделена по профилям и сохраняется между запусками.

### ✅ GUI/UX: MFA и безопасный выход из Garmin — 2026-07-10
MFA-код вводится через окно GUI. Добавлен явный выход из Garmin с очисткой только token-cache
текущего профиля; пароль не сохраняется и очищается при выходе/смене email.

### ✅ GUI/UX: явная проверка Garmin Connect — 2026-07-10
На вкладке Garmin Connect появилась отдельная проверка подключения с понятным статусом.
Она использует общий auth-клиент и не дублирует логику авторизации.

### ✅ GUI/UX: карточка Garmin-профиля и HR onboarding — 2026-07-10
В sidebar виден статус HR-профиля и добавлена явная кнопка повторной настройки. Пользователю
не нужно искать `user_profile.yaml` вручную, чтобы изменить максимальный или покойный пульс.

### ✅ GUI/UX: результат YAML-проверки в верхнем статусе — 2026-07-10
Верхний статус теперь отражает результат команды «Проверить YAML». Невалидный план блокирует
сборку и загрузку, валидный снова разрешает эти действия; подробности ошибки остаются в логе.

### ✅ GUI/UX: статус плана и доступность действий — 2026-07-10
В верхней карточке отображается состояние текущего YAML. Сборка FIT и загрузка в Garmin
заблокированы до успешной загрузки плана, а «Проверить YAML» доступна только при существующем файле.

### ✅ GUI/UX: профили и параметры периода — 2026-07-10
Исправлено раскрытие «Параметры периода» в Simple mode: скрытый разделитель больше не используется
как якорь размещения панели. Для Garmin-профиля добавлена явная кнопка «＋ Новый», пояснение
выбора профиля и очистка пароля при переключении email. Новый профиль создаётся через тот же
профильный store, что и переключение существующего.

### ✅ GUI: foundation нового визуального shell — 2026-07-10
Добавлена общая палитра/типографика в `src/garmin_fit/gui_theme.py`, optional
`customtkinter` с Tkinter fallback, карточка верхнего контекста и увеличены
минимальные размеры окна и быстрые action cards. Бизнес-логика не смешана с
визуальным слоем.

### ✅ GUI/UX: busy-state операций — 2026-07-10
CLI-команды, LLM и Garmin Connect используют `_begin_operation()`/
`_end_operation()`, блокируют конфликтующие действия и показывают статус.
После одиночного и массового удаления Garmin список автоматически перечитывается.
Отмена долгих операций остаётся отдельной задачей.

### ✅ GUI/UX: устранено дублирование действий — 2026-07-10
Основные действия убраны из sidebar и оставлены в едином верхнем блоке быстрых
действий. Контекстные кнопки LLM переименованы в «Собрать этот YAML» и
«Загрузить этот YAML», чтобы не путать их с действиями для текущего плана.
Навигационная кнопка «Открыть конструктор» также удалена: вкладка конструктора
находится рядом и не требует дополнительного дубля.

### ✅ GUI/UX: валидация Конструктора — 2026-07-10
Числовые поля конструктора теперь показывают inline-ошибки и подсветку
невалидного поля. Проверяются целые/вещественные значения, диапазон пульса
30–250, положительные расстояния/длительности и повторы 2–20. Кнопка добавления
блокируется, пока ошибка не исправлена. Добавлено 5 unit-тестов, всего 314.

### ✅ GUI: единые заголовки страниц — 2026-07-10
Для четырёх вкладок добавлены одинаковые page headers с названием, кратким
описанием и цветовым акцентом. Вкладки остаются единственной навигацией, без
дублирующих shortcut-кнопок.

### ✅ GUI/UX: Simple mode — параметры периода скрыты — 2026-07-10
В Simple mode профиль и пароль остаются доступными, а даты, год и dry-run
собраны в раскрывающийся блок «Параметры периода». Expert mode показывает его
автоматически.

### GUI/UX: ошибки ввода в Конструкторе
Заменить беззвучные `except ValueError: pass` на inline-ошибки и сохранение
фокуса в поле. Невалидное число не должно выглядеть как успешно применённое.

### GUI/UX: синхронизация Garmin Connect и черновиков
После одиночного удаления обновлять список и сводку; для временных `_gui_draft`
использовать collision-safe имя и контролируемую очистку старых черновиков.

### GUI/UX: улучшить Simple mode дальше
Сделать валидацию текущего плана визуально заметной в верхней карточке и
связать статус плана с доступностью глобальных действий.

Подробная UX-аудитная точка: `docs/GUI_UX_REVIEW.md`.

### Визуальный конструктор: редактирование существующих тренировок
Всё ещё не начато — см. описание в предыдущей записи (v1 намеренно строит только новые тренировки).

### Реальный remote-хостинг Plan API
Пока проверено только на localhost. Нужно проверить реальный сценарий: API на одной машине,
GUI/бот — на другой (открытый порт, реальный токен, реальная нагрузка).

---

## ✅ Закрыто 2026-07-06 (продолжение) — Фаза B: GUI-фичи поверх PlanStore

- Drag & drop тренировок между днями календаря (клик = детали, драг = перенос)
- Ctrl+Drag = копия на другой день, дедупликация имени при коллизии (`_copy`/`_copy2`)
- Недельный график объёма (км) — canvas bar chart в Календаре
- Toast-уведомления — автоматически из `[OK]`/`[ERR]`/`[WARN]` префиксов `_log()`, видны с любой вкладки
- Личная библиотека шаблонов в Конструкторе (`profile_store.list/save/delete_user_template`)
- Попутно исправлен реальный баг онбординга: `_prompt_hr_profile` падал для каждого
  по-настоящему нового профиля (`pady=(16,4)`-кортеж в конструкторе `tk.Label`)

6 новых тестов, итого 286 проходят.

---

## ✅ Закрыто 2026-07-06 (продолжение) — Backlog cleanup (Фаза A)

- **TODO #6** — таймаут сессии бота: `UserState.last_active`/`onboarded`, сброс через
  `_enforce_session()` при `not onboarded` или простое > `session_timeout_sec` (default 1200,
  настраивается в `bot_config.yaml`). Точка входа — `handle_text_message`;
  `handle_lang_choice` (реальный `/start`) помечает `onboarded=True`.
- **TODO #7** — футер YAML-превью (`yaml_ready_footer`, `yaml_loaded_text_footer`, RU+EN)
  теперь явно перечисляет все три действия: /build, отправить исправленный текст, /cancel.
- **`garmin_auth_manager.prompt_mfa`** — добавлена `prompt_mfa_with_timeout()` (default 120с,
  фоновый поток + `join(timeout)`, `TimeoutError` вместо бесконечного `input()`).
  Заменены оба места (`workflow.py`, `garmin_calendar_export.py`), использовавшие голый `input()`.
- **TODO #10** — тесты error-путей: невалидный YAML/пустой файл/нет workouts/validation errors
  (`load_plan_build_input`), сетевая ошибка при upload/schedule (`garmin_calendar_export`),
  ошибка записи файла (`build_all_fits_from_plan` — падение одной тренировки не прерывает сборку остальных).
- **TODO #11** — вложенный `repeat` в `garmin_step_mapper.map_steps`: подтверждено и закреплено
  тестом корректное поведение (позиционный replay, не иерархическая вложенность — активный/
  восстановительный шаги логично встречаются и отдельно, и внутри вложенной repeat-группы).

18 новых тестов, итого 279 проходят.

---

## ✅ Закрыто 2026-07-06 — Desktop GUI: SQLite-слой, мультипрофили, конструктор

См. подробности в `CLAUDE.md` → «Журнал сессий» → «2026-07-06».

- SQLite-слой (`plan_store.py`) — YAML остаётся каноническим, GUI мутирует через PlanStore
- Мультипрофили по email (`profile_store.py`), онбординг HR-профиля, авто-миграция легаси `user_profile.yaml`
- Визуальный конструктор тренировок без LLM (`workout_builder.py` + вкладка "🧱 Конструктор") — `back_to_offset` вычисляется автоматически, никогда не вводится вручную

## 🟢 Новые задачи (из сессии 2026-07-06)

### Визуальный конструктор: редактирование существующих тренировок
v1 строит только новые тренировки. `PlanStore.move_step`/`delete_step` уже поддерживают
полную логику сдвига `back_to_offset`, но `move_step` в v1 отклоняет перемещение, если в
тренировке уже есть `repeat`-блок — осознанное сужение скоупа, не баг. Если понадобится
редактировать уже закоммиченные тренировки с существующими repeat-группами, нужно решить
неоднозначность "переместить шаг внутрь/наружу существующей группы".

### Единый API над `plan_service.py`
Сейчас GUI и Telegram-бот по отдельности вызывают LLM. План (одобрен, не начат): обернуть
`plan_service.py` в лёгкий HTTP API (вероятно FastAPI, переиспользует Pydantic-схемы),
чтобы бот стал тонким клиентом того же сервиса, а GUI получил режим "LLM автора" —
подключение к чужому хостед-инстансу вместо обязательной настройки своей LLM.

### ~~Drag & drop тренировок между днями календаря~~ ✅ FIXED 2026-07-06

---

## ✅ Закрыто 2026-04-20

### Retry-цикл → thinking mode
- `source_fact_mismatch` теперь демотируется после `_apply_source_fact_consistency_checks()`.
- Cooldown с одним HR-порогом (`до 130`) чинится в repair-слое: `hr_low = 80`.
- Добавлены тесты.

### Telegram UX / надёжность
- Стейл кнопки доставки после рестарта проверяют наличие ZIP.
- `/cancel` во время `generating` ставит флаг, бот останавливается после ответа LLM.
- `reset_state()` удаляет `pending_zip_path`.
- После Garmin-загрузки бот сразу переотправляет клавиатуру если ZIP жив.
- `/send_to_garmin` статус корректно зависит от `path.exists()`.
- Убран мёртвый i18n ключ `delivery_choice_busy`.

### Preview YAML
- YAML превью теперь 3 отдельных сообщения: статус / YAML / footer.
  YAML легко копируется в Telegram.

### Документация
- `docs/TELEGRAM_SETUP.md`, `docs/CHANGELOG.md` обновлены.

---

## ⚠️ Важно: совместимость моделей

### Gemma 4 несовместима с `enable_thinking: false`
`google/gemma-4-e4b` полностью игнорирует параметр `enable_thinking: false`.
При повторной попытке (retry) модель входит в режим размышлений (thinking mode)
и тратит 3000+ секунд вместо ответа.

**Решение:** использовать `qwen3.5-0.8b` или `qwen3.5-4b` в `bot_config.yaml`:
```yaml
llm_model: "qwen3.5-0.8b"
```
`enable_thinking: false` нативно поддерживается Qwen3.

---

## 🔴 Критические (влияют на корректность)

### ~~1. Дублирование `_build_yaml_to_fit_index()` в двух билдерах~~ ✅ FIXED
`build_from_plan.py` и `generate_from_yaml.py` содержат почти идентичные реализации.
CLAUDE.md предупреждает "BOTH must stay in sync" — это значит, что они разойдутся.
**Решение:** вынести в `workout_utils.py`.

### ~~2. `garmin_calendar_export.py`: тихий сбой при парсинге дат~~ ✅ FIXED
`_parse_date()` возвращает `None` на `ValueError`, а `_date_in_range()` трактует `None` как "всегда включать".
Воркаут с нечитаемой датой в имени файла попадёт в любой диапазон без предупреждения.

### ~~3. `logging_utils.py`: дублирование log handler-ов~~ ✅ FIXED
`setup_file_logging()` вызывает `addHandler()` на root logger без проверки на уже добавленный handler.
При повторном вызове в рамках одного процесса все логи пишутся дважды.

### ~~4. Дублирование валидации pace между модулями~~ ✅ FIXED
`_validate_pace()` в `plan_validator.py` и `_is_valid_pace()` в `plan_schema.py` делают одно и то же.
**Решение:** единый `_is_valid_pace()` в `plan_schema.py`, импортируется оттуда.

---

## 🟡 Важные (качество кода и надёжность)

### ~~5. `plan_schema.py`: нет верхней границы для HR~~ ✅ FIXED
`hr_high: int = Field(gt=0)` — Pydantic пропустит `hr_high=500`.
**Решение:** добавить `le=250` на все HR-поля.

### ~~6. `plan_domain.py`: тихое удаление данных~~ ✅ FIXED
`step_from_data()` и `workout_from_data()` молча выбрасывают не-dict элементы из списков без логирования.
При повреждённом YAML пользователь не узнает, что часть тренировок потеряна.

### ~~7. `build_from_plan.py`: нет отката при частичном сбое FIT~~ ✅ FIXED
~~Если FIT-генерация упала на тренировке N, тренировки 1..N-1 остаются в `Output_fit/`.~~
Финальный лог теперь перечисляет имена упавших тренировок и предупреждает о частичных результатах в Output_fit.

### 8. `sbu_block.py`: DEFAULT_DRILLS захардкожены в коде
Пять дефолтных упражнений СБУ вшиты прямо в Python.
**Решение:** вынести в `llm_contract.yaml` или отдельный конфиг-файл рядом с `sbu_block.py`.

### 9. `pipeline_runner.py` — лишняя обёртка
Содержит одну функцию, которая просто вызывает `run_generation_pipeline()` с фиксированными аргументами.
Все вызывающие могут обращаться напрямую к `orchestrator`.

---

## 🔵 Тесты (важные пробелы)

### ~~10. Error paths почти не тестируются~~ ✅ FIXED 2026-07-06
~~Все тесты — happy path. Нет тестов для: невалидный YAML → `load_plan_build_input()`;
сетевая ошибка → `garmin_calendar_export`; ошибка записи файла → `build_from_plan`.~~

### ~~11. Вложенные repeat не тестируются~~ ✅ FIXED 2026-07-06
~~`test_garmin_step_mapper.py` покрывает обычные repeat и SBU, но нет теста для `repeat внутри repeat`.~~

### ~~12. `test_config.py`: побочные эффекты между тестами~~ ✅ FIXED
~~`importlib.reload()` меняет глобальное состояние модуля.~~
Исправлено через `setUp`/`tearDown` с гарантированным `reload` и `os.environ.pop`.

---

## ⚪ Мелкие

- ~~`plan_schema.py`: `check_pace_ordering()` продублирована в `DistPaceStep` и `TimePaceStep` — вынести в `@staticmethod`.~~ ✅ FIXED
- ~~`garmin_auth_manager.py`: `prompt_mfa` — блокирующий вызов без таймаута, CLI может зависнуть.~~ ✅ FIXED 2026-07-06 (`prompt_mfa_with_timeout`)
- ~~`llm/benchmark.py`: путь до фикстур захардкожен — сломается при перемещении `tests/`.~~ ✅ FIXED
- ~~`check_fit.py`: порог предупреждения о размере файла (1 МБ) захардкожен.~~ ✅ FIXED (вынесен в `_LARGE_FILE_BYTES`)

---

## 🟢 Оставшиеся улучшения

### 1. Показывать прогресс во время долгой генерации
Бот молчит 3–12 минут. Нужно: периодически слать «ещё генерирую...»
(`send_chat_action` typing каждые 4 сек, или явное сообщение каждые 60 сек).

### 2. Разрешить отправку плана по частям
Большой план (10+ тренировок) → 8–12 минут. Предупреждение «большой план, ожидайте ~N минут».

### 3. /status показывает технические детали
Сделать human-friendly вариант вывода.

### 4. SBU standard preview без repairs/warnings
В пути "стандарт" `state.yaml_text` показывается без блоков Auto-repair / Warnings.
Фикс: хранить `draft` в `state.pending_draft: Optional[GeneratedYamlResult]`.
Низкий приоритет — пользователь уже видел полное превью до вопроса про СБУ.

### 5. Тест: retry-цикл интеграционный
Подать план с "до 130 уд/мин", убедиться что retry не срабатывает и YAML валиден.

### ~~6. Сессия бота: сброс по таймауту неактивности~~ ✅ FIXED 2026-07-06

### ~~7. UX: подсказка «что делать если YAML неверный»~~ ✅ FIXED 2026-07-06

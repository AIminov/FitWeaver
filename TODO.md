# TODO — FitWeaver

_Обновлено: 2026-04-20_

---

## ✅ Закрыто 2026-04-20

### Retry-цикл → thinking mode

- `source_fact_mismatch` теперь демотируется после `_apply_source_fact_consistency_checks()`,
  поэтому эвристика не может повторно стать blocking validation error.
- Для одиночного верхнего HR-порога (`до 130`, `HR <= 130`) добавлено правило в prompt:
  использовать `hr_low: 80` и `hr_high: <порог>`.
- В YAML repair-слое cooldown-шаги с `hr_low: null` или `hr_low >= hr_high`
  чинятся в диапазон `80-<порог>`, если верхний порог выше 80.
- Добавлены тесты на демоцию `source_fact_mismatch`, prompt rule и repair fallback.

### Telegram UX / надёжность

- Старые кнопки доставки после рестарта бота проверяют наличие ZIP; если файла нет,
  бот просит пересобрать через `/build`.
- `/cancel` во время `generating` ставит `cancel_requested` и останавливает обработку
  сразу после ответа LLM.
- `reset_state()` уже удаляет `pending_zip_path`; дополнительная правка не нужна.

### Документация

- `docs/TELEGRAM_SETUP.md` обновлён после retry-фикса.
- `docs/YAML_GUIDE.md` описывает HR-форматы и одиночный верхний HR-порог.
- `docs/CHANGELOG.md`, `README.md`, `README.ru.md` обновлены.

---

## 🟢 Оставшиеся улучшения

### 5. Показывать прогресс во время долгой генерации
Сейчас бот молчит 3–12 минут. Нужно: периодически слать «ещё генерирую...»
(Telegram `send_chat_action` typing каждые 4 сек, или явное сообщение каждые 60 сек).

### 6. Разрешить отправку плана по частям
Большой план (10+ тренировок) → 8–12 минут. Пользователи не понимают что происходит.
UX: разбить на недели, или добавить предупреждение «большой план, ожидайте ~N минут».

### 7. /status показывает технические детали
Вывод `/status` для обычного пользователя непонятен. Сделать human-friendly вариант,
технический — по флагу (или только для владельца бота).

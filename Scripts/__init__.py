"""Legacy compatibility package.

Primary application code now lives in ``src/garmin_fit``.
Modules in ``Scripts`` remain as thin compatibility shims for older entry
points, tests, and ad hoc local workflows.
"""
